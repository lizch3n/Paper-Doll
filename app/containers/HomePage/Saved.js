import styled from 'styled-components';

const Saved = styled.div`
    background: #e7e7e7;
    padding: 2em 0;
    margin: 5em auto;
    position: relative;
    z-index: 1;
    h3 {
        padding: 0 1em;
    }
    canvas {
        transform: scale(0.9);
    }
`;

export default Saved;
